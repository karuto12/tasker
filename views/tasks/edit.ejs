<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Preview</title>
    <script src="/javascripts/user_script.js"></script>
    <link rel="stylesheet" href="/stylesheets/user_style.css"> <!-- Link to external CSS -->
</head>
<body>
    <div id="main">
        <div id="nav">
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <a href="/logout">Log Out</a>
            <img src="<%= user.image %>" alt="Profile Picture" id="profile_pic">
        </div>
        <div id="tasks_container">
            <div id="form">
                <form action="/user/edit/<%= task._id %>" method="post">
                    <input type="hidden" value="<%= user._id %>" name="userId">
                    <label for="title">Title:</label>
                    <input value="<%= task.title %>" type="text" placeholder="Task Title" id="title" name="title" required>
                    
                    <label for="content">Content:</label>
                    <textarea placeholder="Your Task Description" name="content" id="content"><%= task.content %></textarea>
                    
                    <button type="submit">Update</button>
                </form>
            </div>   
        </div>

    </div>

</html>
